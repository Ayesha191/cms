#!usr\bin python
import sys 
import os
#this program takes command line arguments
#it expects the first argument to select a html template(stored @path)and subsequent inputs to be css styles, applied in order to css stylesheet template
#attached to the html template
path="/" +sys.args[1]
